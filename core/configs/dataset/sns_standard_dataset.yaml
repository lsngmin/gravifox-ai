defaults:
  - base
  - augment: sns_standard
  - _self_

name: sns_standard_dataset
loader:
  batch_size: 256
  num_workers: 20
  prefetch_factor: 8
  drop_last: true
train:
  transforms:
    - type: RandomHorizontalFlip
val:
  transforms:
